import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Free Surplus Funds Claim Review | Rebound Capital Group',
}

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}